// variables 
let compteur = 0; // compteur qui permet de connaître l'image sur laquelle on se trouve 
let timer, plat, slides;

window.onload = () => {
    // on récupère le diaporama 
    const diaporama = document.querySelector(".plats");

    plat = document.querySelector(".plat");

    slides = Array.from(CSSFontPaletteValuesRule.children);


}